import 'package:flutter/material.dart';

class AmbLocation extends StatelessWidget {
  const AmbLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
